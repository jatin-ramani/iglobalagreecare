"use client";import{FormEvent,useState}from"react";export function QuoteForm(){const[status,setStatus]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("Sending…");const f=e.currentTarget,r=await fetch("/api/quote",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(f)))}),j=await r.json();setStatus(j.message);if(r.ok)f.reset()}const x="mt-1.5 w-full rounded-xl border border-forest/15 bg-[#fdfcf9] px-4 py-3 text-sm outline-none focus:border-gold";return <form onSubmit={submit} className="mx-auto grid w-full max-w-xl gap-4 rounded-3xl bg-white p-6 shadow-sm sm:grid-cols-2 sm:p-7"><label className="text-xs font-bold text-ink/70">Name *<input name="name" required className={x}/></label><label className="text-xs font-bold text-ink/70">Phone *<input name="phone" required className={x}/></label><label className="text-xs font-bold text-ink/70 sm:col-span-2">Email *<input name="email" type="email" required className={x}/></label><label className="text-xs font-bold text-ink/70 sm:col-span-2">Message<textarea name="message" rows={3} className={x}/></label><input name="website" className="hidden" tabIndex={-1}/><div className="flex items-center gap-4 sm:col-span-2"><button className="rounded-full bg-forest px-6 py-3 text-xs font-bold text-white" type="submit">Send inquiry →</button><p role="status" className="text-xs text-ink/60">{status}</p></div></form>}
