import{NextResponse}from"next/server";import{Resend}from"resend";const rx=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;export async function POST(req:Request){try{const b=await req.json();if(b.website)return NextResponse.json({message:"Thank you."});if(!b.name||!b.phone||!rx.test(b.email||""))return NextResponse.json({message:"Please complete the required fields."},{status:400});const key=process.env.RESEND_API_KEY;if(!key)return NextResponse.json({message:"Email is not configured."},{status:503});const resend=new Resend(key),from=process.env.EMAIL_FROM||"IGLOBALAGREECARE <onboarding@resend.dev>",to=process.env.BUSINESS_EMAIL||"iglobalagreecare2040@gmail.com";const html=`<h2>New website inquiry</h2><p><b>Name:</b> ${safe(b.name)}</p><p><b>Phone:</b> ${safe(b.phone)}</p><p><b>Email:</b> ${safe(b.email)}</p><p><b>Message:</b> ${safe(b.message||"—")}</p>`;const sent=await resend.emails.send({from,to,subject:"New website inquiry",html});if(sent.error)throw new Error(sent.error.message);return NextResponse.json({message:"Thank you. Your inquiry has been sent."})}catch(e){console.error(e);return NextResponse.json({message:"Please contact us directly."},{status:500})}}function safe(v:unknown){return String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]!))}
